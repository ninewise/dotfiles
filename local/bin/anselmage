#!/bin/sh
[ "$#" -eq 0 ] && exit
torsocks wget "$@"
image="$(basename "$@")"
feh "$image"
mv "$image" /data/pictures/anselmages/
ln -s ../anselmages/"$image" /data/pictures/background-source/"$image"
